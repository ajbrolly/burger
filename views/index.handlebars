<div class="container">
	<div class="row">
		<div class="col-md-6">
			<ul>
				{{#each burgers}} 
					{{#unless devoured}} 
						{{burger_name}} 
					{{/unless}} 
				{{/each}}
				<hr>
			</ul>
		</div>
		<div class="col-md-6">
			<ul>
				{{#each burgers}} 
					{{#if devoured}} 
						{{burger_name}} 
					{{/if}} 
				{{/each}}
				<hr>
			</ul>
		</div>
	</div>
</div>


<div class="container">
	<div class="row">
		<div class="col-md-12">
			<form>
				<label for="body">Enter a Burger You Want to Eat & Click the Submit Button:</label>
				<textarea class="form-control" rows="10" id="body"></textarea>
				<br>
				<button type="submit" class="btn btn-danger btn-lg submit">Submit</button>
			</form>
		</div>
	</div>
</div>